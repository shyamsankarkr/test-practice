- name: testing ansible
  hosts: localhost
  vars_files:
    - variables.yml
  tasks:
    - name: check id exists or not
      ansible.builtin.command:
        cmd: id roboshop
      register: output

    - name: print the output
      ansible.builtin.debug:
        msg: "output is {{ output }}"
